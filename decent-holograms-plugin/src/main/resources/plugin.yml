main: eu.decentsoftware.holograms.DecentHologramsPlugin
version: ${project.version}
api-version: 1.13
prefix: ${project.name}
name: ${project.name}
description: ${project.description}
author: d0by
website: https://www.decentholograms.eu/
softdepend:
  - PlaceholderAPI
  - ProtocolLib
  - Vault
  - LightAPI
  - HeadDatabase
  - ItemsAdder
  - Oraxen

permissions:
  dh.admin:
    description: Allows the holder to use DecentHolograms.
